# Laerlingsportalen 📖

## System ⚙️
📍**React (Vite)** for frontend <br>
📍**Node.js** for backend

## Requirements
📍Node.js <br>
📍npm

## Getting started ⌨️

**1) Install frontend dependencies**
````bash
cd client 
npm install
````
**2) Install backend dependencies**
````bash
cd ../server
npm install
````
## Run the project 
**Frontend**
````bash
cd client
npm run dev
````
**Backend**
````bash
cd server
node app.js
````

## Notes 🔍
- `node_modules`, build files, and .env files are ignored via `.gitignore`.



